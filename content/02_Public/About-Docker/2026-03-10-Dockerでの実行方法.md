1. 実行前の準備としてビルド
	1. -t (-- tag): 作成するイメージに名前とタグをつける
	2. ".": current folderでDockerfileなどを参照しビルドを進める
```
docker build -t [イメージ名] .
```
2. RUN
```
docker run --rm [イメージ名] [引数があれば]
```
	1. --rm: コンテナの実行が終了した直後にそのコンテナを自動的に削除。使い捨てのテストなどで使う